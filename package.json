{
  "name": "vuln-diff-action",
  "version": "2.0.0-alpha.1",
  "description": "Compare vulnerabilities between two refs using Syft+Grype with enhanced HTML/PDF reporting",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npx -y @vercel/ncc build src/index.js -o dist --minify"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/artifact": "^2.1.7",
    "pdf-lib": "^1.17.1",
    "puppeteer": "^24.10.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
