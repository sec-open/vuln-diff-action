{
  "name": "vuln-diff-action",
  "version": "2.0.0-alpha.2",
  "description": "Compare vulnerabilities between two refs (Syft+Grype) and render Markdown/HTML/PDF independently",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx -y @vercel/ncc build src/index.js -o dist -m",
    "lint": "echo 'no lint configured'",
    "test": "node -e \"console.log('ok')\""
  },
  "dependencies": {
    "@actions/artifact": "^2.3.3",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "pdf-lib": "^1.17.1",
    "puppeteer": "^24.10.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
