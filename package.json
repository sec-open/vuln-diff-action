{
  "name": "sec-open-vuln-diff-action",
  "version": "2.0.0-alpha.1",
  "private": true,
  "description": "Phase 1 (Analysis) for sec-open/vuln-diff-action: SBOM (Maven->Syft) + Grype + meta/artifact.",
  "license": "ISC",
  "type": "commonjs",
  "main": ".bundle/index.js",
  "files": [
    "action.yml",
    ".bundle/"
  ],
  "scripts": {
    "build": "ncc build src/index.js -o .bundle --minify",
    "phase1": "node src/index.js",
    "test": "echo \"No tests yet for Phase 1\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@actions/artifact": "^2.2.1",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "puppeteer-core": "^24.15.0",
    "@puppeteer/browsers": "^2.4.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
